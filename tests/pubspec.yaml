name: tests
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.0.0
  flutter_native_splash: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter_native_splash:
  color: "#095A9D"
  image: "assets/images/Teste_splash.png"
  android: true
  ios: false

flutter:
  uses-material-design: true

  assets:
    - assets/images/Teste_splash.png
    - assets/images/Teste.png
